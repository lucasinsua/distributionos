{
  "name": "@prospecting-engine/jobs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npx trigger.dev@latest dev",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@prospecting-engine/shared": "*",
    "@trigger.dev/sdk": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
